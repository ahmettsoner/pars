pipeline {
    agent none

    stages {
        stage('Hello') {
            agent { label 'windows' } 
            steps {
                bat 'echo Hello, World from Jenkins Pipeline on Windows!'
            }
        }
    }
}
