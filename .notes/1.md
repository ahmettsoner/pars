Genel
* `Pars` statik olarak kullanılmamalı uygulama ismi const tan almalı her yerde

Genel
* User friendly message'lar ve error handling iyileştirilmeli

Commands
* Info
* Init
  * Kullanım ile ilgili detaylı farklı senaryolar test edilmeli iyileştirme gerekebilir ama iyi görünüyor
* Environment
  * Incelenmeli, burda ki işleyiş biraz farklı, global flag ile alınıyor yoksa oluşturuluyor burda sadece listeleniyor?
* Group
* Group.List
* Group.Describe
  * Tüm yaml desteği sağlanmalı (-o yaml gibi olaiblir?)
* Group.Remove (src/cmd/group/remove/remove.go)
  * FilePaths ve Names kontrollü şekilde olmalı emin olunmalı
* Submit
  * Apply olarak kullanılmaya devam edecek
  * global bir komut olmalı, resource ten çağırılmamalı `pars project apply` değil `pars apply` olmalı (sonra)

* Commands Option'lar struct olarak tanımlanmalı, alt sistemlere de bu struct'lar geçebilir? (src/cmd/clean/clean.go:50 `project, err := projectService.Clean(name, workspaceName)`)


* `src/core/cmd/common.go:13` ve burda ki diğer func'lar da workspaceName parametresi gereksiz kalabiliyor? incelenmeli `func GetActiveWorkspaceName(workspaceName string) string`

* Aliases
  * `wl` gibi  `p l` `g d` vb komutlar da bileşik kısaltma desteği olmalı çok pratik

Documents
* https://docs.parsdevkit.net/guides/commands/index.html
  * pars info tanımlanmalı
* https://docs.parsdevkit.net/getting-started/concept/workspace.html#definition
  * `Terminology`, `Listing Workspaces` altında olması daha uygun görümnüyor 



## Refactors
* `src/core/cmd/common.go` 
  * `GetActiveWorkspaceNameV2` hazırlandı V1 kaldırılacak!
* `src/modules/services/applicationProjectService.go`
  * `CleanV2` hazırlandı V1 kaldırılacak!

## Browse
### Test
✅ Geçerli (Beklenen) Kullanımlar
| Komut              | Açıklama                                            |
| ------------------ | --------------------------------------------------- |
| `browse`           | ❌ Eksik kullanım — URL belirtilmemiş, hata beklenir |
| `browse url`       | ✅ Argüman olarak URL verilmiş                       |
| `browse --url url` | ✅ Flag ile URL verilmiş                             |
| `browse -u url`    | ✅ Kısa flag ile URL verilmiş                        |
| `browse --url url` | ✅ Uygun flag ile URL verilmiş                       |
| `browse --url url` | ✅ Uygun kullanım, `args` boş                        |
| `browse url`       | ✅ Argümanla, flag olmadan                           |
| `browse --url url` | ✅ Yine geçerli                                      |
| `browse -u url`    | ✅ Aynı şekilde geçerli                              |

❌ Geçersiz / Hatalı Kullanımlar
| Komut                    | Açıklama                                                              |
| ------------------------ | --------------------------------------------------------------------- |
| `browse url arg2`        | ❌ Fazladan argüman (ikiden fazla) → `Args` validation hatası beklenir |
| `browse --url url arg1`  | ❌ `--url` zaten URL taşırken ekstra argüman verilmiş                  |
| `browse -rul`            | ❌ Hatalı flag: `-rul` tanımsız, hata beklenir                         |
| `browse -rul url`        | ❌ Hatalı flag + parametre; yine tanınmayan flag                       |
| `browse --url`           | ❌ `--url` flag’i var ama değeri yok                                   |
| `browse -u`              | ❌ Kısa flag var, ama URL değeri eksik                                 |
| `browse --unknown`       | ❌ Tanınmayan flag                                                     |
| `browse --url=url extra` | ❌ Ekstra argüman verilmiş                                             |
| `browse --url=`          | ❌ URL boş bırakılmış                                                  |


🎯 Test Senaryosu Formatı
| Komut                         | Beklenen Davranış                        |
|------------------------------|------------------------------------------|
| `browse`                     | ❌ URL belirtilmemiş → hata              |
| `browse url`                 | ✅ URL argümandan alınır → başarı        |
| `browse --url url`           | ✅ URL flag'ten alınır → başarı          |
| `browse url arg2`            | ❌ Fazla argüman → hata                  |
| `browse -rul`                | ❌ Tanınmayan flag → hata                |
| `browse -u url`              | ✅ Kısa flag ile URL → başarı            |
| `browse --url`               | ❌ Flag var, değer yok → hata            |
| `browse --url url arg1`      | ❌ Fazla argüman → hata                  |


`/home/ahmettsoner/AS/prj/pars/src/cmd/group/submit/submit.go:63`
group submit -f ../ws/tmp/groups/basic.yaml



**prepareToDescribe gibi bileşenlerde** kind türünden ayrıştyırmak için geçici çözüm uygulandı kalıdıc çözüm planlanmalı örn:
```go
...
if workspace != nil && workspace.Header.Kind == basic_workspace_payload_structs.WORKSPACE_KIND {
...
```