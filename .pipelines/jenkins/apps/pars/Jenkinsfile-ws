pipeline {
    agent none
    stages {
        stage('Check WiX') {
            agent { label 'windows' }
            steps {
                powershell '''
                    wix --version
                '''
            }
        }
    }
}
