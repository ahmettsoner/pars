pipeline {
    agent none
    stages {
        stage('Check WiX') {
            agent { label 'windows' }
            steps {
                powershell '''
                    Write-Host "PATH: $env:PATH"
                '''
                powershell '''
                    wix --version
                '''
            }
        }
    }
}
