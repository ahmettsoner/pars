pipeline {
    agent node
    stages {
        stage('Check WiX') {
            agent { label 'windows' }
            steps {
                powershell '''
                    wix --version
                '''
            }
        }
    }
}
