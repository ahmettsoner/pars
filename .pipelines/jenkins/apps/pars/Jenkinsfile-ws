pipeline {
    agent none
    stages {
        stage('Show PATH') {
            steps {
                powershell '''
                    Write-Host "PATH: $env:PATH"
                '''
            }
        }
        stage('Check WiX') {
            agent { label 'windows' }
            steps {
                powershell '''
                    wix --version
                '''
            }
        }
    }
}
