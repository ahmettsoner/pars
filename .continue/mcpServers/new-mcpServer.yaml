name: New mcpServer
version: 0.0.1
schema: v1
mcpServers:
  - name: Filesystem
    command: npx
    args:
      - "-y"
      - "@modelcontextprotocol/server-filesystem"
      - "${{ inputs.PATH }}"
    env: {}
